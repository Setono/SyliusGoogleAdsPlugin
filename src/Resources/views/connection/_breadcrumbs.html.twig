{# @var resource \Setono\SyliusGoogleAdsPlugin\Model\ConnectionInterface|null #}
{% import '@SyliusAdmin/Macro/breadcrumb.html.twig' as breadcrumb %}

{% set breadcrumbs = [
    { label: 'sylius.ui.administration'|trans, url: path('sylius_admin_dashboard') },
    { label: 'setono_sylius_google_ads.ui.google_ads'|trans, url: path('setono_sylius_google_ads_admin_dashboard') },
    { label: 'setono_sylius_google_ads.ui.connections'|trans, url: path('setono_sylius_google_ads_admin_connection_index') },
]
%}

{% if resource is defined %}
    {% if resource.id is null %}
        {% set breadcrumbs = breadcrumbs|merge([{ label: 'setono_sylius_google_ads.ui.new_connection'|trans }]) %}
    {% else %}
        {% set breadcrumbs = breadcrumbs|merge([{ label: resource.name }, { label: 'setono_sylius_google_ads.ui.edit_connection'|trans }]) %}
    {% endif %}
{% endif %}

{{ breadcrumb.crumble(breadcrumbs) }}
